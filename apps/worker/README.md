# `@autonomous-ux/worker`

Node.js BullMQ worker that runs Playwright / Stagehand UX evaluations.

Depends on `@autonomous-ux/database` for Prisma updates and shared job/issue types.

Scaffolded in **Step 4**. This package is a workspace placeholder until then.
